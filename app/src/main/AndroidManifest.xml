<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="c.core.sample">

    <application
        android:name=".MyAPP"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity.StickyActivity"
            android:exported="true" />
        <activity android:name=".ui.activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity.ViewBindingListActivity"
            android:exported="true" />
        <activity
            android:name=".ui.activity.ListSampleActivity"
            android:exported="true" />
        <activity android:name=".ui.activity.GLoadingActivity" />
        <activity
            android:name=".ui.activity.MagicIndicatorDemoActivity"
            android:exported="true" />
    </application>

</manifest>